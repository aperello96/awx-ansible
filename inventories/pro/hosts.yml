---
# IP de ejemplo: sustituir TODAS las direcciones ansible_host antes de ejecutar.
# Los nombres completos truncados en la captura deben confirmarse también.
# Las credenciales SSH se configuran en AWX, no en este inventario.
all:
  children:
    ubuntu:
      children:
        dns:
          hosts:
            dns01.pro.perelohome.com:
              ansible_host: 10.10.2.14
            dns02.pro.perelohome.com:
              ansible_host: 10.10.2.15
        wireguard:
          hosts:
            wireguard-01.pro.perelohome.com:
              ansible_host: 10.10.2.16
            wireguard-02.pro.perelohome.com:
              ansible_host: 10.10.2.17
        loadbalancer:
          hosts:
            lb-01.okd.pro.perelohome.com:
              ansible_host: 10.10.20.6
            lb-02.okd.pro.perelohome.com:
              ansible_host: 10.10.20.7
        unifi:
          hosts:
            unifi-controller.perelohome.com:
              ansible_host: 192.168.1.23
        ibk:
          hosts:
            ibk.perelohome.com:
              ansible_host: 10.10.2.19
